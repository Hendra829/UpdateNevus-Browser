# Account LFS budget is exhausted — everything committed as regular git blobs.
# Re-enable a rule below (change `-text` to `filter=lfs diff=lfs merge=lfs -text`)
# once the account's LFS pack is topped up.
*.apk -text
*.aab -text
*.zip -text
*.jar -text
*.so  -text
